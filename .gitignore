# Runtime products created on the pod (NOT part of the system itself)
work/
**/auth_state.json
**/screens.json
**/fixtures.json

# The scaffolded React app + deps + build output
node_modules/
dist/
build/
.vite/

# Python / OS noise
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Local-only pod-run notes — reference internal app source files/paths; kept OUT of the public repo.
# Their sanitized, reusable lessons live in skills/legacy-crawl-capture/references/runtime-readiness-and-auth.md
Feedback/
