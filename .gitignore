# Runtime products created on the pod (NOT part of the system itself)
work/
**/auth_state.json
**/screens.json
**/fixtures.json

# The scaffolded React app + deps + build output
node_modules/
dist/
build/
.vite/

# Python / OS noise
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Local-only run notes — reference app source files/paths; never committed.
# Their sanitized, reusable lessons live in skills/legacy-crawl-capture/references/runtime-readiness-and-auth.md
Feedback/
